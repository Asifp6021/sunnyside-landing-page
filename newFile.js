navLinks.forEach(function (navLinks) {
	navLinks.addEventListener('click', function () {
		if (navLinks.classList.contains('.header__nav--link')) {
			navLinks.remove.classList('.header__nav--link');
		}
	});

	navLinks.add;
});
